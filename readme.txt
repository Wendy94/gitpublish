这是对于面试题目的一个简单实现。
js/scrip.js中包含了贝格尔算法的实现。
index.html是界面

第二个版本进行了一些修改：
1、界面有了更多格式
2、增加了“更新按钮”
3、使用了bootstrap的column格式
通过改进，我对jQuery的熟练也更近了一步。

第三个版本：
1、实现了column之间的间隔，实际上是上一个div的padding。
2、更改了颜色，增加了风格。
3、出现对战图时，隐藏队伍名。

第四个版本：
接触了canvas，学习字幕旋转并套用该算法。